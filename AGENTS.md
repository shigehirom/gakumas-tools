# 学園アイドルマスター ツール - エージェント開発ガイド

ようこそ！このドキュメントでは、学園アイドルマスター（Gakumas）ツール CLI を開発する際の主要なパターン、構造、およびワークフローについて説明します。

## プロジェクト概要
このリポジトリは、「学園アイドルマスター」のゲームプレイをシミュレーションし、メモリの組み合わせを最適化するためのツール群を含んでいます。
https://github.com/surisuririsu/gakumas-tools こちらのリポジトリのフォークである https://github.com/shigehirom/gakumas-tools に機能を追加していく形になります。
原則として、surisuririsu さんのリポジトリ由来のソースには手を加えず、shigehirom のリポジトリに対して CLI に関する機能を追加していく形になります。
但し、アイドル画像やスキルカード画像、ステージロジック等、こちらのリポジトリで試験的に実装したものを surisuririsu さんのリポジトリにプルリクを上げたいと思いますので、その際は相談させてください。
shigehirom のリポジトリには、Jules によるレビューも入っています。こちらの内容も適宜取り込む形で改良を進めますので、意識してください。

## 主要な CLI コマンド
- **`contest`**: 特定のステージに対して最適なメモリの組み合わせを探索します。
    - 使用法: `yarn cli contest <stage> [runs] [idolName] [plan] --save`
    - ロジック: メインメモリとサブメモリの全組み合わせを反復処理し、シミュレーションを実行して結果をキャッシュします。
- **`rehearsal`**: 指定された複数のロードアウト（編成）の合計スコアを予測します。
    - 使用法: `yarn cli rehearsal <runs> [...decks]`

## 技術スタックと構成
- **パッケージマネージャー**: Yarn PnP (Plug'n'Play)。スクリプトは常に `yarn node <script>` 経由で実行してください。
- **データベース**: MongoDB。
    - **`loadouts`**: 保存されたアイドルとメモリの編成設定を格納します。
    - **`simulation_results`**: シミュレーション結果をキャッシュし、2回目以降の検索を高速化します。
    - **`memories`**: ユーザーによってアップロードされた生のメモリデータを格納します。
- **フォーマット**: コードの整形には Prettier が使用されます。Handlebars (`.hbs`) テンプレートは、空白文字の崩れを防ぐため `formatOnSave` を無効にすることを推奨します。

## ベストプラクティスとパターン
- **ロードアウトの保存 (Upsert)**: `--save` オプションでロードアウトを保存する際は、`name` と `userId` をキーにして `upsert: true` で `updateOne` を使用してください。これにより、同名のロードアウトが重複して作成されるのを防ぎます。
- **メモリ表示**: レポート表示時には、内部的な MongoDB ID よりも、人間が読みやすい `data.name` (メモリ名) を優先して表示してください。
- **ロギング**: 進捗インジケーターや診断メッセージには `console.error` を使用してください。`stdout` は最終的な JSON やレポート出力のために予約し、スクリプトのパイプ処理や合成を容易にします。
- **アップストリーム同期**: 定期的に `surisuririsu/gakumas-tools` と同期し、最新のシミュレーションロジックやカードデータを取り込んでください。

## 画像アセット管理
- **場所**: `packages/gakumas-images/images/pIdols/` に Pアイドルのアイコンが格納されています。
- **参照**: アイコンは Pアイドルの ID に基づいて命名されています (例: `1.png`, `20.png`)。
- **インポート**: 新しいアイコンを追加した際は、同ディレクトリの `imports.js` を更新する必要があります。

## 既知の注意点 (Gotchas)
- **PnP パス**: 一部のスクリプトは、絶対パスを使用しない場合や、正しい PnP ローダーを使用しない場合に失敗することがあります。
- **メモリの実効ステータス**: サブメモリは、そのパラメータの 20% のみがシミュレーションに寄与します。
