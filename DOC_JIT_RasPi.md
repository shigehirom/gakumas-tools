# Raspberry Pi での JIT 最適化実行ガイド

このドキュメントでは、Raspberry Pi などの非力な環境でシミュレーション（`contest` や `rehearsal`）を高速化するための、実験的な JIT（Just-In-Time）コンパイル機能の使用方法について説明します。

## 概要

従来の `gakumas-engine` は、カードやアイテムの効果（DSL）をシミュレーション実行のたびに解釈（インタプリト）していましたが、JIT モードではこれらを JavaScript の実行可能な関数に動的に変換します。これにより、コアロジックの実行速度が約 5〜10 倍向上します。

## 使用方法

実行時に環境変数 `USE_JIT=1` を指定することで有効になります。

### CLI での使用例

```bash
# rehearsal コマンドでの使用
USE_JIT=1 yarn node packages/cli/bin/cli.mjs rehearsal 1000 my-deck

# contest コマンドでの使用
USE_JIT=1 yarn node packages/cli/bin/cli.mjs contest 42-1 1000 saki
```

### 永続的な有効化（現在のセッションのみ）

```bash
export USE_JIT=1
yarn node packages/cli/bin/cli.mjs rehearsal 1000 my-deck
```

## 注意事項

1. **実験的な実装**:
   - 本機能は現在「実験的」な位置づけです。計算結果の正当性はベンチマークで確認していますが、複雑なカードの組み合わせにおいて予期せぬ挙動が発生する可能性があります。
   - 重要なシミュレーションを行う際は、一度 `USE_JIT=0`（デフォルト）と比較して結果に乖離がないか確認することを推奨します。

2. **メモリ消費**:
   - 初回実行時（カードやアイテムが読み込まれたタイミング）にコンパイルを行うため、わずかにメモリ消費量が増加します。Raspberry Pi 16GB モデルであれば全く問題ありませんが、メモリが極端に少ない環境（512MB以下など）では注意が必要かもしれません。

3. **ブラウザ環境での動作**:
   - Web GUI（ブラウザ）でも動作するように設計されていますが、ブラウザのセキュリティポリシー（CSP）によっては `new Function()` の実行が制限される場合があります。その場合は自動的に従来のインタプリタモードにフォールバックします。

4. **デバッグ**:
   - `USE_JIT=1` でエラーが発生した場合、コンパイルに失敗した DSL がコンソールに警告として表示されます。その場合、該当する効果のみインタプリタモードで実行されます。

## 技術的な詳細

- **コンパイル対象**: カードの使用条件、カード・アイテム・ステージの効果（アクション）、および育成（Growth）効果。
- **最適化手法**: DSL トークンを JavaScript の文字列に展開し、`new Function` で関数化。クロージャによる変数バインドを避け、実行時に必要なステート（`state`, `card`, `resolvers` 等）を引数で渡すことで高速化と正確性を両立しています。
