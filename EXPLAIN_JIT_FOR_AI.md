# AI への JIT 実装説明用プロンプト

このファイルには、Antigravity 上の Gemini などの AI に対し、今回の JIT 実装の内容を説明するためのプロンプトが記載されています。

---

**タイトル: gakumas-engine への実験的な JIT コンパイル層の実装について**

**【概要】**
シミュレーション（contest/rehearsal）の高速化、特に Raspberry Pi などの非力な環境での効率向上を目的に、`gakumas-engine` に実験的な JIT（Just-In-Time）コンパイル層を実装しました。

**【技術的な詳細】**
1.  **Compiler クラスの導入 (`packages/gakumas-engine/engine/Compiler.js`)**:
    *   DSL（トークン配列）を `new Function()` を用いて JavaScript 関数に動的に変換します。
    *   シミュレーション用の `compileAction`、条件判定用の `compileCondition`、および育成効果用の `compileGrowthAction` の 3 種類をサポートしています。
2.  **実行コンテキストの分離**:
    *   カードの効果などは複数のインスタンスで共有されるため、インデックス（`card` など）を関数内に直接埋め込まず（Baking 回避）、実行時に引数として渡す設計にしています。これにより、同一カードの効果を異なるインスタンスに対しても正しく、かつ高速に適用できます。
3.  **コアクラスへの統合**:
    *   `Evaluator.js`: `evaluateCondition` でコンパイル済みの関数があれば優先的に実行。
    *   `Executor.js`: `executeAction` および `executeGrowthAction` で JIT 実行をサポート。
    *   `CardManager.js` / `EffectManager.js`: カードやアイテムの効果が読み込まれたタイミングで「Eager JIT（先行コンパイル）」を行い、結果をトークン配列自体に `.compiled` プロパティとしてキャッシュします。
4.  **環境変数による制御**:
    *   `USE_JIT=1` が設定されている場合のみ有効になります。それ以外、またはコンパイルに失敗した場合は、従来のインタプリタ方式に自動的にフォールバックします。
5.  **互換性への配慮**:
    *   ブラウザ環境でも動作するよう、`process.env` の参照にガードを入れています（CSP の制限がある場合はフォールバックされます）。

**【パフォーマンス改善】**
*   コアとなる条件判定やアクション実行のループにおいて、インタープリタ方式と比較して約 8〜10 倍の速度向上を確認しています。

**【今後の展開】**
*   現在は主要な演算子やアクションに限定していますが、`-=` などの未対応演算子の追加や、中間リゾルバ（`resolveScore` 等）のさらなるインライン化による最適化の余地があります。

この実装に基づき、今後の機能追加やバグ修正を行う際は、JIT モードとインタープリタモードの両方で整合性が保たれるように注意してください。
